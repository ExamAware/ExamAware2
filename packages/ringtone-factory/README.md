# 铃声工厂

ExamAware 官方插件，用于制作可直接导入主程序的 `.ea2r` 铃声包。

## 开发

```bash
pnpm --filter @dsz-examaware/ringtone-factory dev
```

## 构建与打包

```bash
pnpm --filter @dsz-examaware/ringtone-factory build
pnpm --filter @dsz-examaware/ringtone-factory pack-ea2x
```
