import{_ as t}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as n,d as a,o as i}from"./app-jkBS0pEi.js";const o={};function r(s,e){return i(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="reminder-sound-design" tabindex="-1"><a class="header-anchor" href="#reminder-sound-design"><span>Reminder Sound Design</span></a></h1><h2 id="goal" tabindex="-1"><a class="header-anchor" href="#goal"><span>Goal</span></a></h2><p>Finish the existing reminder-sound direction with three bundled event sounds, settings that match the current player settings UI, deterministic playback behavior, and thorough regression coverage.</p><h2 id="existing-direction-preserved" tabindex="-1"><a class="header-anchor" href="#existing-direction-preserved"><span>Existing Direction Preserved</span></a></h2><ul><li><code>ExamPlayer</code> emits <code>colorfulAlert</code> only when a start, ending-soon, or end overlay is actually shown for the first time.</li><li>Desktop settings use <code>player.reminderSound.*</code> and expose a master switch plus per-event switches.</li><li>The desktop renderer owns audio playback; the reusable player package only emits the semantic reminder event.</li><li>Three local assets remain named <code>exam-start.mp3</code>, <code>exam-alert.mp3</code>, and <code>exam-end.mp3</code> under the renderer public directory.</li></ul><h2 id="interaction-design" tabindex="-1"><a class="header-anchor" href="#interaction-design"><span>Interaction Design</span></a></h2><p>The existing “提醒铃声” settings row remains in the “默认参数” card. Enabling it reveals a restrained subgroup consistent with the rest of the settings page:</p><ul><li>A volume row with a slider and numeric percentage.</li><li>Three event rows: 开考铃声, 即将结束铃声, and 结束铃声.</li><li>Each event row has an enable switch and an icon-only preview button with a tooltip.</li><li>Each preview is a real focusable button with a per-sound <code>aria-label</code>; its tooltip opens on hover<br> and focus. Starting playback sets <code>aria-busy</code>, and the button is disabled only for its current<br> start operation. The volume slider has an explicit accessible label and numeric value text.</li><li>Preview is available whenever the master sound switch is enabled, even when that individual event is disabled, so users can evaluate a sound before enabling it.</li><li>Preview buttons have stable dimensions and a loading state while playback starts; only one preview or reminder sound plays at a time.</li></ul><p>No custom file picker, per-event volume, looping, or external sound download is added. Those controls would add storage and validation complexity without supporting the current design direction.</p><h2 id="audio-character" tabindex="-1"><a class="header-anchor" href="#audio-character"><span>Audio Character</span></a></h2><p>Bundle three short, copyright-free generated cues:</p><ul><li>Start: a clean ascending two-note chime, positive but not celebratory.</li><li>Ending soon: a gentle repeated attention chime, distinct without sounding alarming.</li><li>End: a concise descending resolution.</li></ul><p>All assets use consistent loudness and short tails. Default playback volume is 70 percent.</p><h2 id="architecture" tabindex="-1"><a class="header-anchor" href="#architecture"><span>Architecture</span></a></h2><p>Create a renderer-owned <code>reminderSound</code> module with:</p><ul><li><code>ReminderSoundKind</code> and the canonical source map. Sources use renderer-base-relative <code>./audio/...</code><br> paths resolved with <code>new URL(source, document.baseURI)</code>, so HTTP development and packaged Electron<br><code>file://</code> loading both resolve inside the renderer output.</li><li>Settings normalization for master enabled, per-kind enabled flags, and volume. Booleans accept only<br> actual booleans; absent or invalid values use documented defaults. Finite numeric volumes clamp to<br><code>0..1</code>; strings, <code>NaN</code>, and infinities fall back to <code>0.7</code>.</li><li>A controller around an injected audio factory. It lazily creates one audio element per kind, applies current volume before every play, pauses other kinds to prevent overlap, rewinds repeated playback, and exposes <code>play</code>, <code>preview</code>, <code>stop</code>, and <code>dispose</code>.</li><li><code>play(kind, settings)</code> respects master/per-kind settings. <code>preview(kind, volume)</code> intentionally bypasses the per-kind switch but still uses the selected volume.</li><li><code>play</code> and <code>preview</code> return <code>Promise&lt;ReminderSoundResult&gt;</code>. Results are <code>{ ok: true, kind }</code> or<br><code>{ ok: false, kind, reason }</code>, where reason is <code>disabled</code>, <code>superseded</code>, <code>playback-error</code>, or<br><code>disposed</code>. They never throw into the exam event path.</li><li>The optional reporter receives <code>{ kind, phase: &#39;load&#39; | &#39;play&#39;, error }</code>. The controller installs one<br> media <code>error</code> listener per cached element and removes it on disposal. Autoplay rejection uses phase<br><code>play</code> and is logged by the view-level reporter.</li><li>Failure settlement is deduplicated per generation. The first media <code>error</code> event or <code>play()</code> rejection<br> settles that attempt as <code>playback-error</code> and reports once; the other signal is ignored. Error events<br> belonging to stale generations neither settle nor report against the current request.</li><li>Every request increments a monotonic generation. New requests, <code>stop</code>, and <code>dispose</code> invalidate older<br> generations. Stale play-promise settlements pause and rewind their audio and cannot clear or replace<br> the current loading state.</li></ul><p><code>PlayerView</code> owns one controller instance, derives normalized settings reactively, forwards <code>colorfulAlert</code> to <code>play</code>, and disposes the controller on unmount. <code>PlayerSettings</code> owns a separate preview controller and disposes it on unmount.</p><h2 id="event-semantics" tabindex="-1"><a class="header-anchor" href="#event-semantics"><span>Event Semantics</span></a></h2><p>Sound follows the colorful reminder, not the raw exam event. The existing <code>showExamReminder</code> boolean is the single deduplication gate. A sound event is emitted only when the overlay was accepted, so duplicate raw handlers, threshold watches, and status watches cannot produce duplicate sound for the same exam and kind.</p><p>Deduplication is per loaded schedule and exam occurrence. Its key contains kind, stable ID/name, start,<br> and end timestamps. Reused names at different times remain distinct. Replacing the exam configuration<br> clears the deduplication set so reopening a schedule in the same component can notify again.</p><h2 id="failure-handling" tabindex="-1"><a class="header-anchor" href="#failure-handling"><span>Failure Handling</span></a></h2><ul><li>Missing/corrupt asset: report once per failed play attempt, reset the preview loading state, and keep the player functional.</li><li>Browser autoplay rejection: report phase <code>play</code>, log a warning, and continue silently.</li><li>Rapid preview clicks: generation cancellation guarantees only the newest request controls state,<br> including when deferred <code>play()</code> promises settle out of order.</li><li>Component unmount: pause and rewind every cached audio element and release event handlers.</li><li>Invalid persisted volume follows the explicit numeric rules above.</li></ul><h2 id="testing" tabindex="-1"><a class="header-anchor" href="#testing"><span>Testing</span></a></h2><p>Unit tests cover:</p><ul><li>Source mapping and settings normalization, including <code>0</code>, <code>1</code>, fractional, negative, overflow, strings, <code>NaN</code>, and infinities.</li><li>Master and per-kind enable decisions.</li><li>Lazy audio creation, caching, volume application, rewind, single-channel behavior, and preview bypass semantics.</li><li>Deferred play promises resolved out of order, rejected play promises, synchronous throws, media<br><code>error</code> events, stale loading-state protection, stop/dispose invalidation, listener removal, and<br> idempotent disposal. A dedicated test fires media <code>error</code> and play rejection for one generation and<br> asserts one settlement/report, then fires a stale error during a newer request and asserts no effect.</li><li>Mounted component behavior where practical: settings changes react without remount, loading clears<br> on success/failure/stale completion, preview bypasses only the per-kind switch, and unmount disposes<br> controllers. Duplicate player event paths produce one semantic event per occurrence. Source checks<br> may supplement but do not replace observable tests.</li><li>Asset validation with <code>ffprobe</code>: MP3 codec/container, duration from 0.3 to 4 seconds, a nonzero audio<br> stream, and packaged inclusion under <code>dist/renderer/audio</code>. The README records local generation and<br> repository-license provenance.</li></ul><p>Run the complete unit suite, workspace type checks, lint, and production build. Start the desktop development server and use Playwright against the renderer settings route when technically available; otherwise verify the built renderer route in a browser-compatible preview and report any Electron-only limitation explicitly.</p><h2 id="compatibility" tabindex="-1"><a class="header-anchor" href="#compatibility"><span>Compatibility</span></a></h2><ul><li>Existing persisted boolean keys retain their meaning and defaults.</li><li>New <code>player.reminderSound.volume</code> defaults to <code>0.7</code>.</li><li>Existing unfinished source changes are refined in place rather than discarded.</li><li>No Electron sandbox, preload, IPC security, plugin execution, HTTP, or Cast security behavior changes.</li></ul>',28)])])}const c=t(o,[["render",r]]),p=JSON.parse('{"path":"/superpowers/specs/2026-07-11-reminder-sound-design.html","title":"Reminder Sound Design","lang":"zh-CN","frontmatter":{"description":"Reminder Sound Design Goal Finish the existing reminder-sound direction with three bundled event sounds, settings that match the current player settings UI, deterministic playba...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Reminder Sound Design\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-07-11T14:42:28.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"ExamAware Team\\",\\"url\\":\\"https://github.com/ExamAware2\\"}]}"],["meta",{"property":"og:url","content":"https://docs.examaware.com/superpowers/specs/2026-07-11-reminder-sound-design.html"}],["meta",{"property":"og:site_name","content":"ExamAware 2 文档"}],["meta",{"property":"og:title","content":"Reminder Sound Design"}],["meta",{"property":"og:description","content":"Reminder Sound Design Goal Finish the existing reminder-sound direction with three bundled event sounds, settings that match the current player settings UI, deterministic playba..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-07-11T14:42:28.000Z"}],["meta",{"property":"article:modified_time","content":"2026-07-11T14:42:28.000Z"}]]},"git":{"createdTime":1783780948000,"updatedTime":1783780948000,"contributors":[{"name":"hello8693","username":"hello8693","email":"hello8693@hello8693.xyz","commits":1,"url":"https://github.com/hello8693"}]},"readingTime":{"minutes":3.64,"words":1093},"filePathRelative":"superpowers/specs/2026-07-11-reminder-sound-design.md","autoDesc":true}');export{c as comp,p as data};
