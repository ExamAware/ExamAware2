import{_ as t}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as i,d as a,o as n}from"./app-jkBS0pEi.js";const o={};function r(s,e){return n(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="logic-bug-fixes-design" tabindex="-1"><a class="header-anchor" href="#logic-bug-fixes-design"><span>Logic Bug Fixes Design</span></a></h1><h2 id="goal" tabindex="-1"><a class="header-anchor" href="#goal"><span>Goal</span></a></h2><p>Repair the confirmed correctness, lifecycle, persistence, RPC, time-configuration, and lint-tooling defects without completing the unfinished reminder-sound feature or changing Electron security boundaries.</p><h2 id="scope" tabindex="-1"><a class="header-anchor" href="#scope"><span>Scope</span></a></h2><h3 id="included" tabindex="-1"><a class="header-anchor" href="#included"><span>Included</span></a></h3><ol><li>Keep the player’s current exam, sorted exam list, manual switching, status, and emitted events on the same sorted data source.</li><li>Reject invalid exam timestamps and time ranges before they reach the player or task queue.</li><li>Register and unregister the exact same time-provider callback, without accumulating callbacks across repeated starts.</li><li>Treat <code>alertTime</code> consistently as minutes and display the correct “minutes before exam end” message.</li><li>Remove IPC configuration listeners and timers when a load succeeds, fails, times out, or is superseded.</li><li>Make configuration persistence retain changes made while a write is in flight and provide an explicit flush operation for orderly application shutdown.</li><li>Remove an RPC request from the pending map immediately if transport sending throws synchronously.</li><li>Validate NTP interval and numeric configuration before scheduling timers, preventing zero-delay or invalid intervals.</li><li>Migrate the repository’s ESLint configuration to an ESLint 9-compatible flat configuration and make lint commands check-only rather than silently rewriting source.</li></ol><h3 id="explicitly-excluded" tabindex="-1"><a class="header-anchor" href="#explicitly-excluded"><span>Explicitly excluded</span></a></h3><ul><li>Do not add, synthesize, download, or validate reminder-sound audio assets.</li><li>Do not redesign or finish the reminder-sound feature. Preserve the user’s current sound-related source changes.</li><li>Do not change <code>nodeIntegration</code>, sandboxing, context isolation, preload exposure, arbitrary-path file APIs, window security preferences, or plugin execution boundaries.</li><li>Do not add authentication or authorization changes to HTTP, WebSocket, Cast, or plugin routes in this pass.</li><li>Do not perform unrelated refactors or formatting sweeps.</li></ul><h2 id="architecture-and-changes" tabindex="-1"><a class="header-anchor" href="#architecture-and-changes"><span>Architecture and Changes</span></a></h2><h3 id="_1-exam-configuration-validation" tabindex="-1"><a class="header-anchor" href="#_1-exam-configuration-validation"><span>1. Exam configuration validation</span></a></h3><p><code>packages/core/src/parser.ts</code> remains the single basic validation boundary. Validation will parse every start and end timestamp, require both timestamps to be finite, require start to precede end, and require <code>alertTime</code> to be finite and non-negative. Zero remains a valid way to disable the alert. Empty exam lists and optional top-level display strings keep their current core-validator behavior. Overlap detection will use the shared parser rather than a different date-parsing path; because its public contract is boolean, it returns <code>false</code> for invalid ranges while <code>validateExamConfig()</code> is responsible for rejecting them. Adjacent exams where one ends exactly when the next begins do not overlap.</p><p>The player’s detailed validation helper will align with the core validator only for timestamp ordering, timestamp validity, and non-negative finite <code>alertTime</code>. Existing differences concerning empty lists, optional top-level display strings, and the advisory upper bound of 300 minutes remain unchanged in this pass.</p><h3 id="_2-player-state-consistency" tabindex="-1"><a class="header-anchor" href="#_2-player-state-consistency"><span>2. Player state consistency</span></a></h3><p><code>ExamPlayerCore</code> will create one canonical sorted exam sequence per configuration update and use it for index-based operations. <code>currentExam</code>, <code>updateCurrentExam</code>, <code>switchToExam</code>, status computations, and switch events will all resolve indices against that sequence. The original configuration object may remain available for serialization, but it must not be indexed with a sorted index.</p><p>The time-provider change callback will be stored as a class member. <code>start()</code> will register it once, and <code>stop()</code> will unregister that same function. Repeated <code>start()</code> calls remain idempotent.</p><p><code>packages/desktop/src/renderer/src/views/PlayerView.vue</code> will interpret the event’s <code>alertTime</code> argument as minutes and display that the exam will end after the specified number of minutes. Reminder-sound event handling and settings remain untouched.</p><h3 id="_3-configuration-loading-lifecycle" tabindex="-1"><a class="header-anchor" href="#_3-configuration-loading-lifecycle"><span>3. Configuration loading lifecycle</span></a></h3><p><code>ConfigLoader.loadFromIPC()</code> will use one cleanup function that clears the timeout and unregisters the <code>load-config</code> listener. Every terminal path calls cleanup. Starting a new IPC load first rejects the previous promise with a dedicated <code>ConfigLoadCancelledError</code>, performs its cleanup, and only then installs the replacement listener. Each load receives an identity token; late <code>get-config</code> resolutions and stale event callbacks must check the token before changing state or settling a promise.</p><h3 id="_4-configuration-persistence" tabindex="-1"><a class="header-anchor" href="#_4-configuration-persistence"><span>4. Configuration persistence</span></a></h3><p>The configuration store will distinguish scheduled, active, and dirty writes. A mutation during an active write marks the store dirty; after a successful write, completion performs another write using the latest cache when necessary. <code>flushConfig()</code> cancels the debounce timer and continues only while writes succeed and newer dirty changes remain. A failed write rejects the current flush immediately and preserves dirty state for a future caller; it never retries indefinitely within the same call.</p><p>Application shutdown will use a guarded <code>before-quit</code> protocol without changing window or renderer security settings. On the first quit request, the handler prevents default termination, marks flushing in progress, and awaits <code>flushConfig()</code>. It then sets an allow-quit guard and calls <code>app.quit()</code> exactly once; re-entered <code>before-quit</code> observes the guard and proceeds normally. A flush rejection is logged and shutdown still proceeds so a filesystem failure cannot trap the application. Tests cover successful flush, rejected flush, and guarded re-entry.</p><h3 id="_5-rpc-cleanup" tabindex="-1"><a class="header-anchor" href="#_5-rpc-cleanup"><span>5. RPC cleanup</span></a></h3><p><code>JsonRpcClient.request()</code> will wrap <code>transport.send()</code> so a synchronous send failure clears the request timeout, removes the pending entry, and rejects with the original transport error. Normal response, timeout, and disposal behavior remains unchanged.</p><h3 id="_6-ntp-scheduling-validation" tabindex="-1"><a class="header-anchor" href="#_6-ntp-scheduling-validation"><span>6. NTP scheduling validation</span></a></h3><p>Time configuration normalization will cover <code>syncIntervalMinutes</code>, <code>manualOffsetSeconds</code>, and <code>autoIncrementSeconds</code> at <code>loadTimeSyncConfig</code>, <code>applyTimeConfig</code>, and <code>saveTimeSyncConfig</code> entry points. Values must already be finite JavaScript numbers; numeric strings, <code>NaN</code>, and infinities are rejected. <code>syncIntervalMinutes</code> must be greater than zero and falls back to <code>60</code>; both offset fields fall back to <code>0</code>. Normalization happens before <code>setInterval()</code> or offset calculations. This is configuration correctness work only; network protocol and Electron security behavior are unchanged.</p><h3 id="_7-eslint-9-migration" tabindex="-1"><a class="header-anchor" href="#_7-eslint-9-migration"><span>7. ESLint 9 migration</span></a></h3><p>Replace the obsolete <code>.eslintrc.cjs</code>/<code>.eslintignore</code> setup with a flat <code>eslint.config</code> using the installed Vue, TypeScript, Electron Toolkit, and Prettier-compatible configurations. Package lint scripts will be non-mutating. The flat config ignores <code>node_modules</code>, <code>dist</code>, <code>out</code>, generated declaration files, generated plugin/template output, and bundled third-party Doom assets. The existing explicit <code>format</code> command remains the opt-in write operation.</p><h2 id="error-handling" tabindex="-1"><a class="header-anchor" href="#error-handling"><span>Error Handling</span></a></h2><ul><li>Invalid exam dates produce validation failure instead of an incorrect completed status.</li><li>IPC load timeouts reject once and leave no active listener or timer.</li><li>Configuration write failures are logged; dirty state remains eligible for a later flush rather than being silently discarded.</li><li>RPC send failures preserve the original error and leave no pending request.</li><li>Invalid time-sync values use documented safe defaults and never create a zero-delay interval.</li></ul><h2 id="testing-strategy" tabindex="-1"><a class="header-anchor" href="#testing-strategy"><span>Testing Strategy</span></a></h2><p>Add Vitest at the workspace root because the repository currently has no automated tests. Root scripts provide <code>test</code> for a single run and <code>test:watch</code> for development. Logic tests use the Node environment; Vue/renderer modules use lightweight mocks rather than a browser environment unless DOM behavior is directly under test. Every fake-timer suite restores real timers in cleanup.</p><ul><li>Core tests cover invalid timestamps, reversed ranges, negative or non-finite alert values, and overlap behavior.</li><li>Player-core tests cover unsorted configurations, manual switching, emitted switch targets, and time-listener registration/unregistration.</li><li>Config-loader tests use a fake IPC emitter to verify listener cleanup on success, parse failure, timeout, and replacement. Replacement tests assert the cancellation error, both promises’ outcomes, and immunity to late invocation results.</li><li>Config-store tests mock Electron and filesystem boundaries to reproduce mutation-during-write, successful explicit flush, failed flush without looping, and dirty-state preservation.</li><li>RPC tests use a throwing transport and verify immediate rejection and cleanup through observable behavior.</li><li>Time-service tests cover valid numbers plus zero, negative, <code>NaN</code>, infinities, and numeric strings for intervals and both offset fields through extracted normalization logic.</li><li>Shutdown tests cover successful flush, rejected flush, and one guarded re-entry without exercising or changing Electron window security settings.</li><li>Tooling verification runs workspace type checks, lint, relevant tests, and the production build.</li></ul><p>Every production fix follows a red-green cycle: add the smallest regression test, confirm it fails for the expected reason, implement one fix, then rerun the focused and broader suites.</p><h2 id="compatibility-and-change-control" tabindex="-1"><a class="header-anchor" href="#compatibility-and-change-control"><span>Compatibility and Change Control</span></a></h2><ul><li>Existing configuration shapes and public APIs remain compatible, except that invalid configurations are now rejected.</li><li>No reminder-sound file or sound behavior is added.</li><li>Existing uncommitted user changes are preserved; overlapping edits are limited to the alert text/unit bug and are reviewed line by line.</li><li>Security findings excluded above will be reported separately and left unchanged.</li></ul>',35)])])}const d=t(o,[["render",r]]),u=JSON.parse('{"path":"/superpowers/specs/2026-07-10-logic-bug-fixes-design.html","title":"Logic Bug Fixes Design","lang":"zh-CN","frontmatter":{"description":"Logic Bug Fixes Design Goal Repair the confirmed correctness, lifecycle, persistence, RPC, time-configuration, and lint-tooling defects without completing the unfinished reminde...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Logic Bug Fixes Design\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-07-11T09:11:28.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"ExamAware Team\\",\\"url\\":\\"https://github.com/ExamAware2\\"}]}"],["meta",{"property":"og:url","content":"https://docs.examaware.com/superpowers/specs/2026-07-10-logic-bug-fixes-design.html"}],["meta",{"property":"og:site_name","content":"ExamAware 2 文档"}],["meta",{"property":"og:title","content":"Logic Bug Fixes Design"}],["meta",{"property":"og:description","content":"Logic Bug Fixes Design Goal Repair the confirmed correctness, lifecycle, persistence, RPC, time-configuration, and lint-tooling defects without completing the unfinished reminde..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-07-11T09:11:28.000Z"}],["meta",{"property":"article:modified_time","content":"2026-07-11T09:11:28.000Z"}]]},"git":{"createdTime":1783683328000,"updatedTime":1783761088000,"contributors":[{"name":"hello8693","username":"hello8693","email":"hello8693@hello8693.xyz","commits":3,"url":"https://github.com/hello8693"}]},"readingTime":{"minutes":4.46,"words":1337},"filePathRelative":"superpowers/specs/2026-07-10-logic-bug-fixes-design.md","autoDesc":true}');export{d as comp,u as data};
